import { PartOfSpeech } from './part-of-speech.enum';
import { Gender } from '../core';
import { NeuralNetwork } from './neural-network';
import { NeuralNetworkTrainingData } from './neural-network-training-data';
export interface PartOfSpeechRecognitionGuard {
    (word: string, gender: Gender): boolean;
}
export declare class PartOfSpeechRecognitionRule {
    readonly guard: PartOfSpeechRecognitionGuard;
    readonly cache: NeuralNetworkTrainingData;
    private readonly neuralNetwork;
    constructor(guard: PartOfSpeechRecognitionGuard, neuralNetwork: NeuralNetwork, cache: NeuralNetworkTrainingData);
    /**
     * Applies the part of speech recognizer rule to a given word.
     * Returns a part of speech of a given word.
     */
    apply(word: string): PartOfSpeech;
}
//# sourceMappingURL=part-of-speech-recognition-rule.d.ts.map