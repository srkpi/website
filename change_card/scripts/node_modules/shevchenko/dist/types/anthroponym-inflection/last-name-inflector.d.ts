import { InflectorRule } from './inflector.types';
import { Gender, GrammaticalCase } from '../core';
import { PartOfSpeechRecognizer } from '../part-of-speech-recognition';
import { NameInflector } from './name-inflector';
export declare class LastNameInflector extends NameInflector {
    private readonly rules;
    private readonly partOfSpeechRecognizer;
    constructor(rules: InflectorRule[], partOfSpeechRecognizer: PartOfSpeechRecognizer);
    /**
     * @inheritdoc
     */
    protected inflectWord(word: string, gender: Gender, grammaticalCase: GrammaticalCase, isLastWord: boolean): string;
}
//# sourceMappingURL=last-name-inflector.d.ts.map